%h2
  Create Organisation
.row
  = form_for(@user, html: { method: :put, class: 'form-horizontal'}) do |f|
    .col-md-offset-2
      .form-group
        .col-sm-2.control-label
          = f.label :email, for: :email
        .col-md-3
          = f.email_field :email, autofocus: true, class: 'form-control mandatory',  readonly: true

      - if devise_mapping.confirmable? && resource.pending_reconfirmation?
        %div
          Currently waiting confirmation for: #{resource.unconfirmed_email}

      .form-group
        .col-sm-2.control-label
          = f.label :password, for: :password
        .col-md-3
          = f.password_field :password, autocomplete: "off", class: 'form-control'
          .help-block.small (leave blank if you don't want to change it)

      .form-group
        .col-sm-2.control-label
          = f.label :password_confirmation, for: :password_confirmation
        .col-md-3
          = f.password_field :password_confirmation, autocomplete: "off", class: 'form-control'

      .form-group
        .col-sm-2.control-label
          = f.label :current_password, for: :current_password
        .col-md-3
          = f.password_field :current_password, autocomplete: "off", class: 'form-control'
          .help-block.small (we need your current password to confirm your changes)

      .form-group
        .col-sm-offset-2.col-md-2.actions
          = f.submit 'Update', class: "btn btn-primary"
