!!!
%html
  %head
    %title USDN
    = stylesheet_link_tag "application", media: "all", |
      "data-turbolinks-track" => true                  |
    = javascript_include_tag "application", "data-turbolinks-track" => true
    = csrf_meta_tags
    = render 'layouts/shim'
  %body
    - if user_signed_in?
      = render 'layouts/header'
    %p.notice= notice
    %p.alert= alert
    .main
      //parent could be .container
      = yield
    - if user_signed_in?
      .footer
        = render 'layouts/footer'
