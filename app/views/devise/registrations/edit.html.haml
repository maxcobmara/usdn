%h2
  Edit #{resource_name.to_s.humanize}
.row
  = form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put, class: 'form-horizontal'}) do |f|
    = devise_error_messages!
    .col-md-offset-2
      .form-group
        .col-sm-2.control-label
          = f.label :email, for: :email
        .col-md-3
          = f.email_field :email, autofocus: true, class: 'form-control mandatory',  readonly: true

      - if devise_mapping.confirmable? && resource.pending_reconfirmation?
        %div
          Currently waiting confirmation for: #{resource.unconfirmed_email}

      .form-group
        .col-sm-2.control-label
          = f.label :password, for: :password
        .col-md-3
          = f.password_field :password, autocomplete: "off", class: 'form-control'
          .help-block.small (leave blank if you don't want to change it)

      .form-group
        .col-sm-2.control-label
          = f.label :password_confirmation, for: :password_confirmation
        .col-md-3
          = f.password_field :password_confirmation, autocomplete: "off", class: 'form-control'

      .form-group
        .col-sm-2.control-label
          = f.label :current_password, for: :current_password
        .col-md-3
          = f.password_field :current_password, autocomplete: "off", class: 'form-control'
          .help-block.small (we need your current password to confirm your changes)

      .form-group
        .col-sm-offset-2.col-md-2.actions
          = f.submit 'Update', class: "btn btn-primary"

      -if @user.organisation_id.nil?
        .form-group
          .col-sm-offset-2
            You don't have your organisation setup, click <a>here</a> to set it up now.



.col-md-offset-2{style: "display:none"}
  .form-group
    .col-sm-offset-2
      %h3 Unhappy?
      %p
        Cancel my account #{button_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete}
      = link_to "Back", :back
